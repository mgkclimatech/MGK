# Agent notes

- Stack: TanStack Start (React 19) + Vite + Tailwind CSS v4, package manager is Bun.
- `bun run dev` starts the dev server on port 8080; `bun run build` produces the
  production bundle; `bun run lint` / `bun run format` before committing.
- Keep `main` in a working state — commits pushed there deploy directly.
